scripts:
    start:
        cmd: ./src/app.ts
        tsconfig: tsconfig.json
        allow:
            - net
            - read
    reload: deno run --reload --config tsconfig.json --allow-net --allow-read ./src/app.ts